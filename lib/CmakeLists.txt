set(IMGUI_PATH ${CMAKE_CURRENT_LIST_DIR}/imgui)
message(${IMGUI_PATH})
    
# Compile as static library 
file(GLOB IMGUI_SRC ${IMGUI_PATH}/*.cpp ${IMGUI_PATH}/*.h ${IMGUI_PATH}/backends/imgui_impl_sdl2.cpp ${IMGUI_PATH}/backends/imgui_impl_sdlrenderer2.cpp) 

add_library(Imgui STATIC ${IMGUI_SRC})
target_include_directories(Imgui PUBLIC ${IMGUI_PATH})

add_subdirectory(nativefiledialog-cmake)